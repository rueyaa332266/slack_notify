name: Slack Demo
on:
  pull_request:
    branches: [master]
jobs:
  GitHub-Actions-job-1:
    runs-on: ubuntu-latest
    env:
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
    steps:
      - name: update demo
        run: |
          curl -X POST -d \
          "payload={
            'username': 'Hands-on Demo',
            'channel': 'D049PEKKHFC',
            'text': '1st 123 \
            \n2nd 456 \
            \n3rd'
          }" \
          $SLACK_WEBHOOK
